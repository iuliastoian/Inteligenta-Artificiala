# command for creating executable python file: python main.py
# change rights so that all users can execute the file: chmod a+x main.py
# execute the file: ./main.py

# age = int(raw_input("How old are you? "))

"""if (age > 0) and (age < 120):
    if (age == 21):
        print "Same as me"
    elif (age > 21):
        print "Older than me"
    else:
        print "Younger than me"
else:
    print "Don't lie about your age"""""

# Conditional Expresion

x, y = 1, 0

a = 'y is less than x' if (y < x) else 'x is less than y'

print a